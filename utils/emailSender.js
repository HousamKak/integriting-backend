// utils/emailSender.js
async function sendEmail({ to, subject, text, html }) {
    // Placeholder function for sending emails.
    // In a real system, integrate with Nodemailer or a transactional email service
    console.log(`Pretend to send email: to=${to}, subject=${subject}`);
  }
  
  module.exports = {
    sendEmail
  };
  